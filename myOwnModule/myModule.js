// Check If "title" and "content" data is undefine or not
// If undefine => alert "There must be a title"
export function ifUndifined(title, content) {
    if (title === "" || content === "") {
        alert("There must be a title and content, please check again!!!")
    }else{}
}